{
  "name": "short-url-proxy",
  "version": "1.0.0",
  "description": "A serverless function to shorten URLs",
  "main": "api/shorten-url.js",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}